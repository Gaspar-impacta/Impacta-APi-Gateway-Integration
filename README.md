# Rodando kong com postgresql via docker compose

Dentro da pasta raiz do projeto, execute:

# Para rodar o projeto
1- ./start.sh 

# Para parar o projeto, mantendo a persistência dos dados no banc
2- ./stop.sh 
